<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.xx.demo.dao.mapper.redis.RedisMapper">

	<select id="getRedisMasters" resultType="RedisMaster">
		select * from redis_master
	</select>
	
	<select id="getRedisSlaves" resultType="RedisSlave">
		select * from redis_slave
	</select>

</mapper>